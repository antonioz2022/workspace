{
  "name": "cortex-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:local": "node build.mjs --local",
    "smoke": "node smoke-test.mjs",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
